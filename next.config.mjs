/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : [
            'images.unsplash.com',
            'www.istockphoto.com',
            'media.istockphoto.com'
        ]
    }
};

export default nextConfig;
